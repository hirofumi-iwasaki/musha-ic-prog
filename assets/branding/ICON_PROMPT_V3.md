# IC Programmer icon v3

Tool: built-in image_gen.

## Initial pin-count correction

Use case: precise-object-edit. Make ONE localized correction to this exact app icon: fix the EPROM's REAR/UPPER row of silver pins to match the existing FRONT/LOWER row. The existing LOWER row has exactly TWELVE (12) pins; preserve those 12 lower pins unchanged. Replace the current 10 upper pins with exactly TWELVE (12) evenly spaced upper pins, forming twelve opposing pairs along the two long edges of the chip (24 pins total). Upper pins should sit directly opposite the lower pin positions across the chip, approximately at x=205,284,363,442,522,600,680,759,839,919,998,1078 on the original 1254px-wide canvas, with small natural perspective adjustment only if necessary. All 12 upper pins must visibly protrude above the chip's white rim and be separately countable; preserve their short rear-view appearance. No pins on the two short sides. Do not change chip size or position, the 12 lower pins, notch, white outline, quartz window/die, castle, red stone, stone wall, cream interior, outer rounded border, composition, colors, lighting, or textures. Preserve actual transparent alpha outside the icon; do not draw a checkerboard. No text or numbers. Before finishing ensure both rows have exactly 12 distinct pins.

## Follow-up correction

Precise localized correction: the attached icon currently has THIRTEEN short silver pins along the UPPER edge of the EPROM, but TWELVE long pins along the LOWER edge. Fix ONLY the upper pins. The upper row must have TWELVE pins total: SIX in the left half and SIX in the right half, with NO pin at the horizontal center above the quartz window. Delete the extra central upper pin, then redistribute the remaining twelve uniformly along the upper edge so that each is directly opposite one of the existing twelve lower pins. Count as 6 + 6 = 12. Keep the LOWER row exactly unchanged at 12. Do not add any other pin. Keep absolutely everything else unchanged: large IC body, white rim, notch, quartz window, castle, cream backdrop, red/dark stone, rounded border, canvas, actual exterior transparency. Only the small upper pin row is edited. No text, labels, or watermark.

## Final local insertion

Strict local inpainting: the upper edge of the IC has a large EMPTY GAP in its middle, between the five left silver tabs and the five right silver tabs. ADD ONLY TWO matching short silver pin tabs in that empty gap, at canvas x approximately 580 and 667, y approximately 718 (1254px square source). Their tops and bottoms must match the adjacent existing upper tabs. The result is the existing 5 left tabs + 2 new middle tabs + existing 5 right tabs = 12 upper pins. Keep EVERY existing upper pin unchanged. Keep all 12 lower pins unchanged. Do not remove, shift or redraw existing pins. Edit ONLY the two tiny insertion areas immediately above the IC's upper white rim. Preserve every other part of the icon exactly, including chip, white outline, quartz window, castle, stone, red accent, background, outer rounded border and transparent exterior. No text.

Visual inspection: 12 upper pins and 12 lower pins.
