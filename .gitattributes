# Native patches and build scripts must match upstream LF source on every host.
* text=auto eol=lf
*.png binary
*.ico binary
*.icns binary
*.tar.gz binary
*.zip binary

# Context lines inside unified diffs intentionally start with a space.
*.patch whitespace=-blank-at-eol,-space-before-tab
